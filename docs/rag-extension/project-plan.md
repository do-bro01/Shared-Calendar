# SC RAG 확장 — 프로젝트 계획서

> 작성일: 2026-05-15
> 버전: v0.1
> 관련 문서: [아이디어 초안](idea-draft.md), [DB 설계 문서](db-design.md)

---

## 1. 프로젝트 정보

| 항목          | 내용                                                       |
| ------------- | ---------------------------------------------------------- |
| 프로젝트명    | SC: 추억을 검색하는 AI 캘린더                              |
| 과목          | (수업명 입력)                                              |
| 팀명          | (팀명 입력)                                                |
| 팀원          | (팀원 입력)                                                |
| 기간          | (시작일) ~ (종료일)                                        |
| 발표일        | (발표일)                                                   |
| 기반 프로젝트 | [SC MVP](../spec-sc.md) (Expo + Supabase 기반 공유 캘린더) |

---

## 2. 프로젝트 목표

1. **일정 + 사진 + 코멘트 데이터를 자연어로 검색하는 RAG 챗봇** 구현
2. **개인/그룹 단위 권한 격리 검색** (pgvector + RLS)
3. **RAGAS 기반 정량 평가** (Ground Truth 15개 이상)
4. **모바일·웹 통합 UI** 구현 (기존 React Native 앱에 챗봇 화면 추가)
5. **(가점)** RAG 성능 개선 기법 적용 (Hybrid Search, Re-ranking 등)

---

## 3. 수업 요구사항 매핑

| 요구사항                           | 충족 방법                                 | 산출물           |
| ---------------------------------- | ----------------------------------------- | ---------------- |
| RAG가 필요한 서비스 기획           | "개인 일상 데이터 검색"이라는 새 영역     | 아이디어 초안    |
| RAG 필요성 어필 (vs 순수 LLM 비교) | RAGAS 정량 비교 + 시나리오 정성 비교      | 평가 보고서      |
| 서비스 대상자 명확                 | 페르소나 3종 정의 (연인/가족/친구)        | 아이디어 초안 §3 |
| RAG 기본                           | LangChain 표준 패턴                       | 코드             |
| 벡터 DB                            | Supabase pgvector                         | DB 설계 문서     |
| Memory 기능                        | `chat_messages` 테이블 + 세션별 히스토리  | 코드             |
| 챗봇 기능                          | 신규 챗봇 화면 (RN 앱, 선택적으로 Streamlit) | UI               |
| Github 업로드                      | 코드 + README + 모든 문서                 | 레포             |
| UI 기획·구현                       | 챗봇 화면 + 추억 피드 화면 신규           | 디자인 + 코드    |
| RAGAS 평가                         | Ground Truth 15개, 메트릭 4종, 비교군 3개 | 평가 보고서      |
| 배포                               | Vercel (기존 RN 웹) + Supabase Edge Function | 배포 URL         |
| 논문 제출 (가점)                   | "개인 데이터 RAG의 권한 격리 패턴" 주제   | 논문 초안        |
| 성능 개선 노력 (가점)              | Hybrid Search + Re-ranking 적용           | 코드 + 비교 결과 |

---

## 4. 시스템 아키텍처

```
┌───────────────────────────────────────────────────────────┐
│                    Client Layer                           │
│  ┌──────────────────────┐    ┌────────────────────────┐   │
│  │ React Native + Expo  │    │ Streamlit (선택, 옵션)   │   │
│  │ TypeScript - 메인     │    │ Python - 발표 시각화용    │   │
│  └──────────┬───────────┘    └────────────┬───────────┘   │
└─────────────┼─────────────────────────────┼───────────────┘
              │                             │
              ▼                             ▼
┌─────────────────────────────────────────────────────────────┐
│                    API Layer                                 │
│  ┌──────────────────────────────────────────────────────┐  │
│  │ Supabase Edge Function (Deno)                        │  │
│  │  - LLM API 키 보호                                    │  │
│  │  - RAG 파이프라인 (검색 → 컨텍스트 → 생성)            │  │
│  │  - Rate limit (사용자당 일 N회)                       │  │
│  └──────────────────────────────────────────────────────┘  │
└─────────────┬─────────────────────────────┬───────────────┘
              │                             │
              ▼                             ▼
┌─────────────────────────────────────────────────────────────┐
│                  Data & Service Layer                        │
│  ┌────────────────────┐  ┌──────────────────────────┐      │
│  │ Supabase Postgres  │  │ External APIs            │      │
│  │ + pgvector         │  │  - OpenAI (Embedding)    │      │
│  │ + RLS              │  │  - Claude/GPT (LLM)      │      │
│  │ + Storage          │  │  - Vision API (Caption)  │      │
│  └────────────────────┘  └──────────────────────────┘      │
└─────────────────────────────────────────────────────────────┘
```

### 4.1 데이터 흐름 (수집)

1. 사용자가 일정/사진/코멘트 등록
2. Supabase Trigger 또는 Edge Function이 변경 감지
3. OpenAI Embedding API로 임베딩 생성 (텍스트 + 사진 캡션)
4. `embeddings` 컬럼/테이블에 저장

### 4.2 데이터 흐름 (검색)

1. 사용자가 챗봇에 자연어 질문 입력
2. Edge Function이 질문을 임베딩 변환
3. pgvector 유사도 검색 (RLS로 권한 격리 자동 적용)
4. 상위 K개 결과를 LLM 컨텍스트에 주입
5. LLM이 답변 생성 + 출처 정보 포함
6. 응답 + 검색된 청크 ID를 `chat_messages`에 저장 (Memory)

---

## 5. 기술 스택

### 5.1 언어 전략

| 구역 | 언어 | 이유 |
|------|------|------|
| 프로덕션 코드 (앱 + RAG 백엔드) | **TypeScript / JavaScript** | 기존 RN 앱 + Supabase Edge Function(Deno) 모두 TS. 단일 언어 유지 |
| 평가 스크립트 | **Python** | RAGAS가 Python 라이브러리이므로 평가 부분만 Python (1회성) |
| (선택) 합성 데이터 생성 | Python 또는 TS | 어느 쪽이든. faker + OpenAI는 Python이 약간 편함 |

> **RAG는 언어와 무관하다.** 패턴(임베딩→검색→생성)만 맞으면 어느 언어로든 구현 가능. OpenAI/Anthropic SDK와 pgvector는 양쪽 모두 지원. LangChain도 JS 버전(`@langchain/core`) 존재. 이 프로젝트 규모면 LangChain 없이 직접 구현해도 충분.

### 5.2 스택 상세

| 영역                   | 선택                                                  | 이유                                            |
| ---------------------- | ----------------------------------------------------- | ----------------------------------------------- |
| **Frontend (메인)**    | React Native + Expo + react-native-web (TS)           | 기존 MVP 코드 재사용, 모바일/웹 동시 지원       |
| **Backend / RAG 파이프라인** | Supabase Edge Function (Deno + TypeScript)      | 기존 인프라 통합, RLS·인증 자동, 별도 서버 불필요 |
| **Vector DB**          | Supabase pgvector                                     | 별도 벡터 DB 도입 불필요, RLS와 자연스럽게 결합 |
| **Embedding 모델**     | OpenAI `text-embedding-3-small` (1536차원)            | $0.02/M 토큰, 한국어 품질 양호                  |
| **LLM (생성)**         | Claude Haiku 4.5 또는 GPT-4o-mini                     | 비용 효율 + 한국어 우수, 챗봇용 충분            |
| **Vision (사진 캡션)** | Claude Sonnet Vision 또는 GPT-4o Vision               | 업로드 1회만 호출 (캐시)                        |
| **RAG 프레임워크**     | 없음 (직접 구현) 또는 LangChain.js (선택)             | 규모상 직접 구현이 가벼움. 복잡해지면 LC.js 도입 |
| **평가**               | **RAGAS (Python)**                                    | 수업 요구사항. 별도 Python 스크립트로 분리      |
| **Re-ranking (가점)**  | Cohere Rerank API 또는 BGE-Reranker                   | 검색 정확도 향상. API 호출이라 언어 무관         |
| **(선택) 데모 페이지** | Streamlit (Python) — 옵션                             | RN 앱으로 충분히 발표 가능. 시간 여유 시 추가  |
| **배포**               | Vercel (RN 웹) + Supabase                             | 무료 티어 활용                                  |
| **모니터링**           | Supabase Dashboard + LangSmith (옵션)                 | 호출 로그·비용 추적                             |

---

## 6. 주요 기능 (신규)

### 6.1 사진 업로드

- 일정 카드에서 사진 추가
- Supabase Storage에 저장
- 업로드 직후 Vision API로 캡션 자동 생성 → DB 저장 → 임베딩 생성

### 6.2 코멘트

- 일정에 멤버들이 코멘트 추가
- 작성 즉시 임베딩 생성
- 실시간 동기화 (Supabase Realtime)

### 6.3 챗봇 화면 (메인 신규)

- 자연어 입력
- 응답에 출처 일정 카드를 함께 표시
- 카드 탭 시 해당 일정 상세로 이동
- 대화 히스토리 좌측 사이드바

### 6.4 추억 피드 화면 (선택)

- 지난 일정 중 사진/코멘트가 있는 것만 시간순 표시
- LLM 월별 요약 노출

### 6.5 검색 범위 선택

- "내 데이터만" / "○○ 그룹"
- RLS와 함께 검색 쿼리에서 명시적 필터링

---

## 7. 일정 (8주 기준 예시)

| 주차 | 기간   | 작업                                            | 담당 | 산출물                    |
| ---- | ------ | ----------------------------------------------- | ---- | ------------------------- |
| 1주  | (날짜) | 기획 확정, 아이디어/계획/DB 문서 마무리         | 전원 | 본 문서들                 |
| 2주  |        | DB 스키마 확장 + 합성 데이터 생성 스크립트      | A    | 마이그레이션 SQL, seed.py |
| 3주  |        | 임베딩 파이프라인 (Edge Function) + Vision 캡션 | A, B | 코드                      |
| 4주  |        | RAG 검색 함수 + 챗봇 API                        | B    | Edge Function             |
| 5주  |        | 챗봇 UI (RN 앱) + (선택) Streamlit 데모          | C    | 화면                      |
| 6주  |        | Memory 기능, 출처 카드 표시, Hybrid Search      | B, C | 통합 데모                 |
| 7주  |        | RAGAS 평가, Re-ranking 적용, 성능 비교          | A    | 평가 보고서               |
| 8주  |        | 발표 자료, 데모 영상, 논문 초안                 | 전원 | PPT, MP4, 논문            |

> 4주 진행 시: 6~7주차 작업을 압축하거나 일부 가점 항목(Re-ranking, 논문)을 빼고 진행.

---

## 8. 역할 분담 (3인 기준)

| 담당                           | 역할                          | 주요 작업                                           |
| ------------------------------ | ----------------------------- | --------------------------------------------------- |
| **팀원 A — Data/RAG Engineer** | 데이터 파이프라인, RAGAS 평가 | 합성 데이터, 임베딩 생성, 평가 스크립트, 성능 분석  |
| **팀원 B — Backend Engineer**  | RAG 백엔드, API               | Edge Function, 검색 로직, Hybrid Search, Re-ranking |
| **팀원 C — Frontend Engineer** | UI/UX, 챗봇 화면              | RN 챗봇 화면, Streamlit 데모, 출처 카드, 추억 피드  |

> 1인 추가 시: **팀원 D — Documentation/Presentation** 분리 (발표 자료, 논문, 사용자 시나리오 작성)

> 발표 자료·논문은 전원이 자기 파트를 글로 정리한 뒤 최종 통합.

---

## 9. 평가 계획 (RAGAS)

### 9.1 Ground Truth 구성 (15개 이상)

| 분류                  | 개수 | 예시                                 |
| --------------------- | ---- | ------------------------------------ |
| 사실 기반 (날짜/장소) | 6    | "2025년 7월에 다녀온 여행지는?"      |
| 사실 기반 (인물/관계) | 3    | "엄마와 함께한 일정 중 가장 최근은?" |
| 통계/패턴             | 3    | "지난 1년 동안 가장 자주 간 식당은?" |
| 멀티모달 (사진 기반)  | 3    | "벚꽃 사진 찍은 날 어디였지?"        |

### 9.2 메트릭

| 메트릭            | 목표   | 측정 방법 |
| ----------------- | ------ | --------- |
| Faithfulness      | ≥ 0.85 | RAGAS     |
| Answer Relevancy  | ≥ 0.80 | RAGAS     |
| Context Precision | ≥ 0.75 | RAGAS     |
| Context Recall    | ≥ 0.80 | RAGAS     |

### 9.3 비교군

1. **Baseline**: 순수 LLM (전체 일정을 텍스트 dump해서 컨텍스트에 넣음)
2. **RAG Basic**: 단순 벡터 검색 + LLM
3. **RAG Advanced**: Hybrid Search (BM25 + Vector) + Re-ranking + LLM

각 비교군에 대해 동일한 Ground Truth로 정확도/응답시간/토큰비용 측정.

---

## 10. 위험 요소 및 대응

| 위험                    | 영향             | 대응                                                    |
| ----------------------- | ---------------- | ------------------------------------------------------- |
| 신규 사용자 데이터 부족 | RAG 데모 불가    | **합성 데이터** 생성 (가상 사용자 2년치 500건)          |
| 임베딩 비용 폭주        | 비용             | 사용자당 데이터 양이 적음 (월 < $0.01 예상) + 배치 처리 |
| LLM API 키 노출         | 보안             | 클라이언트 직접 호출 금지, Edge Function 프록시 강제    |
| RAGAS 평가 시간         | 일정 지연        | 평가 자동화 스크립트 + 비교군 병렬 실행                 |
| 팀 협업 충돌            | 개발 지연        | Git Flow 명시, 주 1회 동기화 미팅, PR 리뷰 의무         |
| 한국어 임베딩 품질      | 검색 정확도 저하 | OpenAI 모델과 BGE-Korean 비교 후 선택                   |
| TS/Python 환경 분리     | 컨텍스트 스위칭  | 프로덕션은 TS만, Python은 평가/seed 1회용 스크립트로 한정 |

---

## 11. 산출물 체크리스트

### 코드

- [ ] 임베딩 파이프라인 (Supabase Edge Function, TS)
- [ ] RAG 검색 + LLM 호출 API (Supabase Edge Function, TS)
- [ ] 챗봇 UI (React Native, TS)
- [ ] 합성 데이터 생성 스크립트 (Python 또는 TS)
- [ ] RAGAS 평가 스크립트 (**Python 필수**)
- [ ] (선택) Streamlit 데모 페이지 (Python)

### 문서

- [ ] 아이디어 초안 ([idea-draft.md](idea-draft.md))
- [ ] 프로젝트 계획서 (본 문서)
- [ ] DB 설계 문서 ([db-design.md](db-design.md))
- [ ] 평가 보고서 (RAGAS 결과)
- [ ] README (실행 방법)
- [ ] (가점) 논문 초안

### 배포

- [ ] Vercel: RN 웹 앱 URL
- [ ] Supabase: DB + Edge Function
- [ ] (선택) Streamlit Cloud: 데모 페이지 URL

### 발표

- [ ] PPT
- [ ] 데모 영상 (3~5분)
- [ ] Q&A 예상 질문/답변

---

## 12. 변경 이력

| 날짜       | 버전 | 변경 내용 | 작성자   |
| ---------- | ---- | --------- | -------- |
| 2026-05-15 | v0.1 | 최초 작성 | (작성자) |
